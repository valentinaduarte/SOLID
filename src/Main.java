public class Main {
    public static void main(String[] args) {
        //EJercicio 6 
        /*
         * TaxStrategy USA = new UsaTaxStrategy();
         * TaxStrategy UK = new UKTaxStrategy();
         * TaxCalculator calculator = new TaxCalculator();
         * calculator.calculateTax(USA);
         * calculator.calculateTax(UK);
         */
    }
}